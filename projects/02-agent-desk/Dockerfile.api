FROM python:3.12-slim
WORKDIR /app
COPY api/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY api/ .
ENV PYTHONPATH=/app
EXPOSE 8200
CMD ["uvicorn", "desk.app:app", "--host", "0.0.0.0", "--port", "8200"]
